# script 说明（Lua 分离原则）

- Phase 0-2 目标纯 DB，不放 Lua。
- 若 WoM 分档需要脚本，只允许脚本负责“识别档位并应用隐藏 effect”，
  不把伤害公式写进 Lua（任务书 §8、§13）。
- `campaign/` 放战役快照逻辑（若需要），`battle/` 放战斗逻辑（首版避免）。
- 任何 Lua 方案必须与 DB 数据分离，方便一键退回纯 DB 版（§9.1）。
- 当前两目录留空占位。
