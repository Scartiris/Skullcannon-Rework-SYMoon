# 施工日志（Build Log）

> 动手前必须以 2026 年当前游戏版本 `db.pack` / Assembly Kit 为最终准绳（任务书 §2）。
> 基线数据源：`T:\Program Files (x86)\Steam\steamapps\common\Total War WARHAMMER III\data\db.pack`

## 基线复核（Base Game Baseline，db.pack 实测 2026-09-06）

> 工具：RPFM v5.0.6 Server（WebSocket API）+ `tools/rpfm_dump.py`，`container=db.pack`。
> 反查脚本与方法见 `tools/README.md`。

| 项目 | 任务书 v1.0 公开值 | db.pack 实测 |
|---|---|---|
| 单位 DB key | `wh3_main_kho_veh_skullcannon_0` | 一致（`main_units_tables` v7，2 行命中含 RoR `wh3_dlc26_kho_veh_skullcannon_ror`） |
| 本地招募时间 | 2 回合 | 一致（`create_time` 2） |
| 招募费用 | 1100 | 一致 |
| 维护费 | 275 | 一致 |
| 生命值 | 4700 | 基本一致（`land_units.bonus_hit_points` 4684 + 实体基础，总计约 4700，待游戏内确认） |
| 当前解锁建筑 | T4 Foundry of Blood | **有出入**：实际招募行在 `wh3_main_kho_vehicle_3`（`building_units_allowed_tables` v4）；`wh3_main_kho_vehicle_2` 有一行但 `enabled=false` |
| 建筑链 | T3 Foundry of Scythes → T4 Blood → T5 Skulls | **不存在**：`foundry` 仅命中帝国努恩炮厂；恐虐载具链为 `wh3_main_kho_vehicle`：`vehicle_1`（level 0，需主城 3）/ `vehicle_2`（level 1，需主城 4）/ `vehicle_3`（level 2，需主城 5） |
| 建筑 WoM 储量变化 | T3 +2 / T4 +4 / T5 +8 | 待查（`building_effects`，Phase 2 前确认） |
| land_unit 关联 | — | mount `wh3_main_kho_mnt_skullcannon` → entity `wh3_main_kho_vehicle_skullcannon`；近战武器 `wh3_main_kho_skullcannon`；远程武器 `wh3_main_kho_skullcannon_skull` |
| 弹药 | — | `primary_ammo` 12（首版目标 10 左右，Phase 1 调）；`use_secondary_ammo_pool=false`（主弹药池，切换不刷弹药前提成立） |
| 装甲/近战 | — | `wh2_main_heavy_metal_110`（110 甲）；近战 30/18，士气 60 |
| 机动 entity（`battle_entities_tables` v39） | — | run 6.8 / walk 2.0 / charge 9.0 / turn 60 / mass 1600 / very_large |
| 原版弹丸（`projectiles_tables` v53） | — | `wh3_main_kho_skullcannon_skull`：`artillery_default` + `low` 弹道，射程 380 / 死区 10，伤害 88+352AP，装填 11.0，散布 275/8.5，穿透 `medium_3`，建筑伤 ×1.5，可弹跳 |
| 高抛参照（帝国迫击炮 `wh_emp_mortar_shell`） | — | `artillery_explosive` + `fixed` 弹道，射程 380 / 死区 60，仰角 56/固定 50，`gravity` -1，散布 300/380，穿透 `low`，不可弹跳 |
| 原版能力 | — | `wh3_main_unit_passive_skullfeast`（5s active / 20s recharge / 无限次 / 自身）：phase `replenish_ammo` 0.1（每周期回 10% 弹药，数值已接近 §5.3 目标），`heal_amount` 0；另有 `gorefeast`、`daemonic_instability`、`single_entity` |
| AI | — | `ai_usage_group` = `hybrid_direct_shock_arty` |

### 与任务书的差异（必须正视）
1. **建筑链命名**：任务书 §2 的 Foundry of Scythes/Blood/Skulls 在当前 db.pack 无此 chain，实际为 `wh3_main_kho_vehicle_1/2/3`（T3/T4/T5）。显示名待 loc 核对，Phase 1/2 按实际 key 施工，任务书 §4 的“T3/T4/T5”映射为 vehicle_1/2/3。
2. **当前解锁等级**：任务书称 T4，实测有效招募行在 vehicle_3（T5 级）；vehicle_2 有禁用行。Phase 1“T3 下放”实际是从 vehicle_3 复制招募行到 vehicle_1（T3 级）。
3. **Skullfeast 已存在**：Phase 1 无需新建补给机制，复用原版被动，首版只调触发/数值（若需），T5 再提补弹效率。

## 反查方法（Method）

1. RPFM 打开 `db.pack`，Global Search `wh3_main_kho_veh_skullcannon_0`，
   记录 `main_units / land_units / battle_entities / building_units_allowed` 等真实表名。
2. 从 `Foundry of Blood` 反向找 `building_effects_junctions + effects`。
3. 把实际表名和 key 回填上表；与任务书 §7.2 不一致处以实测为准。

## Phase 记录（Phase Records）

### Phase 0：技术 Spike（DB 构建完成，待游戏内验证）
- 路线：方案 A 纯 DB。依据：`battle_unitcontroller:change_shot_type` 为引擎原生命令；
  `projectile_shot_type_enum` 15 种 shot（含 `artillery_default`/`artillery_explosive`，均免 effect 解锁）；
  活范例 DLC25 大炮葡萄弹（`wh3_dlc25_great_cannon_grape_shot`，`artillery_canister`，
  与 default 弹共享 `projectile_display`，`projectile_shot_type_display` 非 placeholder）。
- 假设：同 `projectile_display` 下多 shot_type → 引擎自动给切换 UI。
- pack：`pack/skc_skullcannon_rework.pack`（Mod/PFH5，构建脚本 `tools/rpfm_pack.py`，回读验证通过）。
- 内容（`source/db/*.tsv`，仅目标行）：
  - `skc_rework_projectile_assault`：复刻原版，仅射程 380→280（隔离变量）。
  - `skc_rework_projectile_siege`：`artillery_explosive` + `fixed` 弹道，射程 500 / 死区 90，
    仰角 56/固定 50，`gravity` -1，散布 380，穿透 low，不弹跳，装填 11→16；伤害暂保持 88+352AP。
  - `skc_rework_missile_skullcannon`：default=assault。
  - `land_units` 原版行 `primary_missile_weapon` 临时指向新 weapon（spike 用，Phase 1 建独立单位后移除）。
- 游戏内验证（待用户执行，见 §验收清单 10.2 前 4 项）：
  1. pack 复制到 `.../Total War WARHAMMER III/data/`，自定义战斗选恐虐带颅骨魔炮；
  2. 看有无弹药切换按钮；有则来回切 10 次，记录射程/弹道/弹药/HP/经验变化；
  3. **若无按钮**：改 `projectile_shot_type_display` 非 placeholder 后再测；仍无则转方案 B（Lua `change_shot_type`）。
- 2026-09-06 修订（对标死亡尖啸火箭 `deathshrieker_cluster/demolition` 双按钮）：
  主弹也给了非 placeholder 显示项（`skc_rework_assault_shot`），图标换成已验证的
  `cannon_default/cannon_canister`（之前 `artillery_explosive` 在 displays 表无用例），
  并新增 `text/db/skc_rework.loc`（双按钮中英名称+说明）。用户首测只出了无描述坏按钮，修完待复测。
- 2026-09-06 实质系统（用户要求真切换 + 架设固定）：
  [已废弃，见下] 舞姿式双能力 + Lua 弹种同步。
- 2026-09-06 变身版（用户拍板要彻底方案；对标万变魔君/paperpancake/Caesar）：
  - 攻城形态是完整独立单位：`main/land skc_rework_skullcannon_siege` + 坐骑 + **速度锁零 entity** +
    攻城专属武器（默认攻城弹，无切换按钮，形态即弹种）。
  - 往返变身能力（behaviour 留空 + spawned_unit 指 land key，已验证可行）：
    `skc_rework_deploy_siege`（前摇 5s）/`skc_rework_undeploy`（前摇 4s），
    `active_time -1` 无限驻留（待实测；若自动收回则改 3600），共享血量疲劳开，
    冷却 30s/10s 防刷，挂载到各自形态 + 原版被动全保留。
  - Lua `script/battle/mod/skc_rework.lua` 改弹药血量桥：变身新生单位只许往下对齐旧弹药/旧血量
    （杜绝 Caesar 式变身回满刷血），稳态零操作；StyLua 过语法。
  - 文本：部署/收炮中英名 + 说明、攻城形态单位名。能力图标暂空（Phase 5 补 UI 资源）。
- 2026-09-06 根因（RPFM DiagnosticsCheck 实锤）：
  战斗能力 key 必须在战役 `unit_abilities` 表有对应行，否则启动 fail-fast（`InvalidReference`）。
  paperpancake 的 key 原版自带对应行，Caesar 自建了对应行，我之前两边都没对上。
  已补 `unit_abilities`（图标 guerrilla_deploy / redeploy，原版 UI 资源）+
  描述文本注册表，诊断零 InvalidReference。另：能力图标不再空白。
- 2026-09-06 数值轮（用户验收后）：
  - 基础弹药 12→20（双形态；Lua 桥按比例同步，不受总量影响；Skullfeast 10% 回弹现约 2 发）。
  - 攻城弹：爆炸半径 6→7、爆炸伤 70→85/AP 20→25、bonus_v_infantry +40；
    突击弹：独立小爆炸（半径 3→2），穿甲直射定位不变。
  - 战斗 Lua 轮询 2s→1s（变身同步延迟减半；5s 架设前摇为设计意图，保留）。
  - T4（vehicle_2）/T5（vehicle_3）加成：破甲 +25/+50、弹药 +20/+40（弹药语义按百分比假设，
    20 基础对应 +4/+8 发，实测单位卡弹药数校准）。纯 DB 无法表达不叠加，
    改 Lua 战役计数器独占施加隐藏包（T5 优先，多建筑/跨省/拆楼均单份，来回切换不叠层）。
- 2026-09-06 I18N（用户拍板：英文进 db 表，中文进 localisation 表）：
  - 查实原版布局：英文 235 个 `text/db/*.loc`，中文 1 个 19 万行 `text/localisation__.loc`，同 key。
  - 教训：`localisation__.loc` 同路径为整体替换语义，6 行小文件曾清空中文 UI；
    已改中文文件全量复制底表 + 追加我们行（`tools/loc_export_cn.py` 可重导，生成物不进仓）。
  - 文案按原版风味重写（名极简、描述一句风味一句机制），`loc/skc_rework_text.csv` 为唯一源，
    同时生成中英两文件。加成包标题因无 key 机制，暂留中英短串。
