# pack 说明

- 本 MOD 独立 pack，建议命名 `skc_skullcannon_rework.pack`（`skc_rework_` 前缀体系）。
- 构建出的 `.pack` 放此目录本地测试，进游戏时复制到
  `.../Total War WARHAMMER III/data/` 即可。
- 二进制不进 git（见根 `.gitignore`），仓库只留 `source/db` 的 TSV 文本。
